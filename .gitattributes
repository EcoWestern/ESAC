# Normalise text files to LF in the repository and in the working tree, so that a
# specification amended on one platform is byte-identical when checked out on another.
* text=auto eol=lf

*.png binary
*.jpg binary
*.webp binary

# Machine-maintained files: keep them out of language statistics.
.github/workflows/* linguist-generated=true
